*.parquet
*.pdf
*.png
data/
.DS_Store
models/
old/
dr_app/
docs/
__pycache__/
.ipynb_checkpoints/
catboost_info/
paper/

# W&B: filled-in config (credentials) and run scratch; the .example is committed.
scripts/config/wandb.yaml
wandb/

# Run logs from long-running experiment sweeps (see MODELING.md)
scripts/logs/

# Design notes: kept local. Records lab-server specifics (hostnames,
# scratch paths, node layout) that do not belong in a published repo.
MODELING.md